<!DOCTYPE html>

<html lang="{{ global.request.locale }}">

<head>

    <!-- Bootstrap -->
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head, any other head content must come *after* these tags -->

    {% block head %}{% endblock %}

    <!-- Combined Bootstrap and ARK CSS -->
    <link href="{{ absolute_url(asset('styles/ark.min.css', 'frontend')) }}" rel="stylesheet" type="text/css"/>

</head>

<body>

    {% block body %}{% endblock %}

    <!-- Combined jQuery, Bootstrap and ARK js -->
    <script src="{{ absolute_url(asset('scripts/ark.min.js', 'frontend')) }}"></script>

</body>

</html>
