
{% if global.user %}

    <p>{{ 'core.user.login.loggedin' | trans({ '%name%': global.user.displayName }) }}</p>
    <p><a href="{{ path('user.logout') }}">{{ 'core.user.reset.logout' | translate() }}</a></p>

{% else %}

    <h2>{{ 'core.user.reset.heading' | translate }}</h2>

    <p class="text-muted lead">
        {{ 'core.user.reset.notregistered' | translate }}
        <a href="{{ path('user.register') }}">{{ 'core.user.reset.register' | translate }}</a>
    </p>

    <p class="text-muted">
        {{ 'core.user.reset.reset' | translate }}
    </p>

    {% include "layouts/grid.html.twig" %}

{% endif %}
