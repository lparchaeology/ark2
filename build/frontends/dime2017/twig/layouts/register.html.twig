{# ARK Form template for standard Bootstrap grid #}

{% if element.type.id == 'form' and form %}
    {{ form_start(form) }}
{% endif %}

{% include 'layouts/grid.html.twig' %}

{% if element.type.id == 'form' and form %}
    {{ form_end(form) }}
{% endif %}
