{% block image -%}

{% if state.modus == 'active' %}

    {{ form_row(form) }}

{% else %}

    <div class='item'>
        <img src="{{ data.value.sourcePath('thumb') }}" alt='' />
    </div>

    {% do form.setRendered %}

{% endif %}

{%- endblock image %}
