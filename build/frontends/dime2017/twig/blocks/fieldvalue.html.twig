{# Block to write out correctly formatted field value #}
{% block fieldvalue %}
    
   {{ item.name }}
    {{ dump (state.modus) }}
    {% if item.property(field.attribute.name).value is not null %}
            {{ item.property(field.attribute.name).value['content'] }}
        {% else %}
            {{ field.attribute.name }}
        {% endif %}

{% endblock fieldvalue %}
