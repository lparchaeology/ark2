{# Block to write out correctly formatted field value #}
{% block fieldvalue %}

    {{ item.name }}
    {% if item.value(field.attribute.name) is not null %}
        {{ item.value(field.attribute.name).content }}
    {% else %}
        {{ field.attribute.name }}
    {% endif %}

{% endblock fieldvalue %}
