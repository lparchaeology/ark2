{# Define the standard notification block #}
{% block notification %}

    <a class = "messagetext" href="{{ path('dime.home.messages' ) }}">
        {{ notification.event.term | translate }}
        <br/>
        {{ notification.event.object.schema | translate ~ ' ' ~  notification.event.object.id }}
    </a>
    <span class="markasread" href="{{ path('dime.api.message.read') }}"
    message="{{ notification.id }}"
    recipient="{{ workflow.actor.id }}">
        &times;
    </span>

{% endblock notification %}
