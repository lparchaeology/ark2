{% extends "ark_main_layout.html.twig" %}

{% block content %}

<div class="container-fluid">

    <div class="alert alert-info" role="alert">Hey there, here's an alert...</div>

    <div class="row col-md-12 form-inline form-group">
        {{ form_start(item_form) }}
        {{ form_widget(item_form) }}
        {{ form_end(item_form) }}
    </div>

    {% for element in layout.elements %}
        {% include element.template() with {'layout': element, 'forms': forms[element.id]} only %}
    {% endfor %}

</div>

{% endblock %}
